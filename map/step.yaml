# JSON schema for the path node
step:
  type: object
  additionalProperties: false
  properties:
    meta:
      type: object
      additionalProperties: false
      properties:
        title:
          type: string
        description:
          type: string
    required:
      - title
