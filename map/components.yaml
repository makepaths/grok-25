$schema: "http://json-schema.org/draft-07/schema#"
definitions:
  Meta:
    type: object
    additionalProperties: false
    properties:
      title:
        type: string
      description:
        type: string
    required:
      - title
